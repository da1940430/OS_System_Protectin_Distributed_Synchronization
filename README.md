# OS_System_Protectin_Distributed_Synchronization
Implement a server and a client by socket.  
  
It could upload and download.  
  
In addition, it also could create user and permission(read and write).
